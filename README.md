# React + Vite

To run this project locally, open the project folder in VS Code. Then, open the terminal and execute the following commands: run npm install to install dependencies, followed by npm run dev to start the development server.
